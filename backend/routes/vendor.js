const express = require("express");
const { addProduct } = require("../controllers/product");
const router = express.Router();

// router.route('/product').post(addProduct).get(getProducts);
module.exports = router;
